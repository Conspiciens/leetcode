#include <iostream> 
#include <string> 

vector<int> getRow(int rowIndex){
    vector<int> triangle(2); 
    
    triangle[0] = 1; 
    if (rowIndex == 1) 
        return triangle; 
    
    triangle[0] = 1; 
    for (int i = 0; i < rowIndex; i++){
        triangle        
    } 
} 

int main() {
    getRow(3); 
} 
